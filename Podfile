# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppStore-Rx' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AppStore-Rx

    pod 'RxSwift', '~> 6.0.0'
    pod 'RxCocoa', '~> 6.0.0'
    pod 'Kingfisher', '~> 6.0'

  target 'AppStore-RxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AppStore-RxUITests' do
    # Pods for testing
  end

end
